---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: fakebook-profile
  labels:
    app: fakebook-profile
    role: db
spec:
  storageClassName: default
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
